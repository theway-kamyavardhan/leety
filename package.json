{
  "name": "dsa-grind-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Static local-first DSA, LeetCode, contest, hackathon, and job application tracker.",
  "type": "commonjs",
  "scripts": {
    "start": "node api-server.js",
    "dev": "concurrently \"node api-server.js\" \"vite\"",
    "build": "vite build",
    "check": "node -c api-server.js && node -c api/ai-coach.js && node -c api/hackathons.js && node -c api/leetcode.js && node scripts/validate-sheets.js",
    "test": "vitest run",
    "vercel-build": "npm run build"
  },
  "keywords": [
    "dsa",
    "leetcode",
    "tracker",
    "static",
    "vercel"
  ],
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "concurrently": "^9.0.0",
    "jsdom": "^25.0.0",
    "postcss": "^8.5.14",
    "tailwindcss": "^3.4.17",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
